interface:
  display_name: "Provider Switch"
  short_description: "Safely manage agent and model provider configurations."
  default_prompt: "Use $provider-switch to install or switch a cataloged Agent/Provider profile such as codex-ds-pro, claude-ds-pro, or claude-step; preserve login and provider credential boundaries, and report dedicated YOLO permissions plus any profile-scoped context guardrails."
