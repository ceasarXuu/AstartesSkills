interface:
  display_name: "Provider Switch"
  short_description: "Safely manage agent and model provider configurations."
  default_prompt: "Use $provider-switch to install or switch a cataloged Agent/Provider profile, preserve login and credential boundaries, and explicitly report when a dedicated launcher defaults to YOLO or bypass-permissions mode."
