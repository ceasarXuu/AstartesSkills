interface:
  display_name: "Roles Install"
  short_description: "Install shared global roles across local coding agents."
  default_prompt: "Use $roles-install to collect my global coding-agent roles, detect supported agents installed on this machine, show and confirm the effective install scope, then install the roles safely and report restart requirements."
