# Pitch Templates

## 15-Second Pitch

`[Project]` helps `[user]` stop `[old pain]` by `[new mechanism]`, so they can `[main outcome]` faster and with less friction.

## 60-Second Pitch

`[Project]` is built for `[user]`, who currently deal with `[painful workflow]`. Instead of `[old way]`, it provides `[new path]`. What makes it credible is `[implemented evidence]`, and what makes it interesting commercially is `[differentiated wedge or expansion path]`. Right now it is at `[stage]`, with `[next step]` as the near-term expansion.
